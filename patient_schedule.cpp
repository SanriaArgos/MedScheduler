#include "patient_schedule.hpp"
#include "utils.hpp"
#include <iostream>

// Клиентская интерактивная логика удалена – используйте JSON интерфейс вместо этого.
bool view_doctor_schedule_for_patient(database_handler &db) {
    std::cerr << "view_doctor_schedule_for_patient: client-side schedule selection removed. Use JSON interface instead.\n";
    return false;
}

#include "registration.hpp"
#include "utils.hpp"
#include <iostream>

bool registration() {
    std::cerr << "Registration function is not available in JSON mode. Use the JSON interface instead.\n";
    return false;
}
